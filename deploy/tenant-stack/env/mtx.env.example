# MediaMTX + Guard — serviço mtx no compose tenant
# Segredos: copiar RTMP_PUBLISH_SECRET do Go (igual worker/Rust)

RTMP_PUBLISH_SECRET=SUBSTITUA_PELO_SEGREDO_RTMP

# Opcional — ver DEPLOY_EASYPANEL.md confvision
# MTX_LOG_FILE=/recordings/mediamtx.log
